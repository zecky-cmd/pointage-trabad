
# Installation des bibliothèques (excel pdf)

```bash
npm install jspdf jspdf-autotable
npm install xlsx
```
# Ajouter les boutons d'export

```bash
npm install jspdf jspdf-autotable xlsx
npm install --save-dev @types/jspdf-autotable
```

```bash
npm install react-toastify
```
```bash
npm install
npm install -D tailwindcss@3.4.4 postcss@8.4.31 autoprefixer@10.4.16 
npx tailwindcss@3.4.4 init -p --ts
npm install lucide-react
npx shadcn@latest init
```